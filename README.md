Small Node-Red script that calls from a Weather API and creates a graph of the humidity of the selected location.
The file contains the necessary configurations for MQTT output nodes, including topics and client IDs. Please ensure to replace the placeholder values with your actual channel and client ID information before deploying the flow.
